# ESPHome RF Bridge v1.3.26

Smart SRX882 burst recording with STX882 raw replay.

The recorder may be armed for several seconds, but only the best discrete RF burst is retained. Idle SRX882 chatter outside the selected burst is discarded. The ESPHome YAML/API from v1.3.25 remains unchanged.
